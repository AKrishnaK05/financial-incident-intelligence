# Core deterministic pipeline uses only the Python standard library.

# Testing
pytest>=8.0

# Structured LLM output
pydantic>=2.7

# API
fastapi>=0.110
uvicorn>=0.29

# UI
streamlit>=1.32
pandas>=2.2

# AI providers (only needed when the corresponding provider is selected)
google-genai>=1.0
openai>=1.30
